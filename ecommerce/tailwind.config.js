/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This ensures Tailwind looks for class names in JS, JSX, TS, and TSX files inside the src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
